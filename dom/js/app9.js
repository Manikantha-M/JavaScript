// textContent
/* textContent property returns the text content of an element and also the text content of its descendant elements. 
textContent property is also used to set the text content of an element. */

const txt = document.getElementById('special');
console.log(txt);
const val = txt.textContent;
console.log(val);

const item = document.querySelector('div');
console.log(item);
const res = item.textContent;
console.log(res);
