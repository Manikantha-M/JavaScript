// Template literals - ES6+
/* Template literals are enclosed by the backtick (` `) character instead of double or single quotes. */

const nam = 'john';
const age = 25;
const s = `Hey it's ${nam} and he is ${age} years old.`;
console.log(s);
const a = `${4 + 4}`;
console.log(a);
